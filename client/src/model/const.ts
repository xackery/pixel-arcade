/**
 * @author       Digitsensitive <digit.sensitivee@gmail.com>
 * @copyright    2018 Digitsensitive
 * @description  Asteroid: Const
 * @license      Digitsensitive
 */

export let CONST = {
  SHIP_SIZE: 20,
  SCORE: 0,
  HIGHSCORE: 0,
  LIVES: 3,   
};

export let GEM_SIZE = 64;
export let GEM_SPACING = 2;
export let GEM_SIZE_SPACED = GEM_SIZE + GEM_SPACING;
export let BOARD_COLS;
export let BOARD_ROWS;
export let MATCH_MIN = 3; // min number of same color gems required in a row to be considered a match
